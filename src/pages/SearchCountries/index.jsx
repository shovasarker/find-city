import React from 'react'
import { useParams } from 'react-router-dom'

const SearchCountries = () => {
  //const { countryName } = useParams()
  return <div>SearchCountries</div>
}

export default SearchCountries
